// import API from './api.js';
// import UI from './ui.js';

// const Weather = {
//   async fetchWeather(location) {
//     try {
//       const weatherData = await API.fetchWeatherData(location);
//       UI.displayWeather(weatherData); // Display the fetched weather data using the UI module
//     } catch (error) {
//       UI.displayMessage('Failed to fetch weather data');
//     }
//   },
// };

// export default Weather;
